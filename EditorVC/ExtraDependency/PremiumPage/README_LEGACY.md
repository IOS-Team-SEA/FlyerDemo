# Legacy Premium Page

This directory contains the previous in-app purchase implementation (`IAPView`, `IAPViewModel`, and related helpers). The new paywall architecture lives under `VideoInvitation/PremiumRevamp`. Keep these files for reference while the rollout completes, but avoid adding new logic here.
